name: Keep Server Alive

# 每 10 分钟执行一次
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch: # 允许手动触发

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl --request GET \
               --url "https://websarvice-190762-4-1381433578.sh.run.tcloudbase.com/avatar" \
               --header "User-Agent: GitHub Actions Keep-Alive"